# 私聊 & 群聊三种模式分析文档

---

## 一、产品设计理念：三种模式的定位

### 核心理念

产品模拟的是「你和角色之间的互动」，互动方式分三种：

| 模式 | 本质定位 | 用户感受 |
|---|---|---|
| **线上** | 远程文字聊天（微信/短信/通讯终端） | 你看不到对方表情动作，只能通过文字交流 |
| **线下** | 面对面互动（同处一个空间） | 你能看到对方的表情、动作、周围环境 |
| **导演** | 用户主导的场景演绎（用户描述场景，角色在场景中行动） | 用户像导演一样搭建舞台，角色在舞台上自然表演 |

### 三种模式的共性与差异

**共性**：
- 每一轮回复都可以分段（多句 dialogue、多处 narration）
- 模型以 JSON 格式输出结构化内容

**差异**：

| 维度 | 线上 | 线下 | 导演 |
|---|---|---|---|
| 是否有旁白 | 禁止 | ✅ 必须有 | ✅ 必须有 |
| 场景约束 | 远程通讯终端 | 同一物理空间 | 用户文字描述的场景 |
| 旁白内容 | — | 动作/神态/环境 | 动作/神态/环境 |
| 对话方式 | 纯文字消息 | 当面说的话 | 当面说的话 |
| 可否看到对方 | ❌ 不能 | ✅ 能 | ✅ 根据场景描述 |

### 群聊模式的定位

群聊是多个角色 + 用户同处一个交流场景。三个模式的定义和私聊一致：

| 模式 | 场景 |
|---|---|
| **群聊线上** | 所有人在群里发文字消息，通过终端交流 |
| **群聊线下** | 所有人聚在一起当面聊天 |
| **群聊导演** | 用户描述一个场景，所有人在这场景中互动 |

---

## 二、三种模式展示规则（想要的）

### 私聊展示规则

| 模式 | 旁白展示 | 对话展示 | 格式 |
|---|---|---|---|
| **线上** | 不展示 | 用户发的文字消息样式 | `{"segments":[{"type":"dialogue","content":"..."}]}` |
| **线下** | 灰色旁白文字，标注"旁白" | 普通消息样式 | `{"segments":[{"type":"narration","content":"..."},{"type":"dialogue","content":"..."}]}` |
| **导演** | 同线下 | 同线下 | 同线下 |

**分段规则**：所有模式均可分段，一段 narration 后跟多段 dialogue，或交替出现。

### 群聊展示规则

| 模式 | 旁白展示 | 对话展示 | 格式 |
|---|---|---|---|
| **线上** | 不展示 | 每条显示发言人名字+消息内容 | `[{"speaker":"甲","message":"...","type":"dialogue"}]` |
| **线下** | 灰色文字，显示"旁白" | 同线上 | 同上数组格式，可含旁白 |
| **导演** | 同线下 | 同线上 | 同线下 |

### 理想的行为约束（提示词层面）

**私聊**：
- 线上：只能输出 dialogue，禁止 narration
- 线下：建议至少一段 narration + 至少一段 dialogue，最后一段建议是 dialogue
- 导演：同上

**群聊**：
- 线上：所有成员只能用 dialogue，禁止旁白
- 线下：建议至少一条旁白，不强制
- 导演：同线下
- 全体：每轮每个角色至少发言一次（禁言成员已被过滤，不在名单中）

### 理想的行为约束（代码层面）

**代码只做三件事**：
1. 解析模型输出的 JSON，提取内容
2. 根据模式决定 UI 展示（线上隐藏旁白、线下显示旁白）
3. 遇到格式错误时有限容错（cleanJson 清理格式、回退解析），但**绝不生成假内容填充**

**代码不做的事**：
- ❌ 不硬塞对话（如「嗯，我在听。」）
- ❌ 不强制校验结构（缺旁白、缺对话、末段不是对话都不拦截）
- ❌ 不因为格式不符合预期就重试纠正（重试只在 API 层面：空回复、超时、完全无法解析）

---

## 三、当前提示词的问题与缺陷

### 问题 1：群聊线下/导演 type 字段说明自相矛盾

**位置**：`PromptTemplates.kt:370`（线下），`478`（导演）

```
type：固定填"narration"（仅旁白条目需要此字段，干员发言不需要）
```

但输出示例（`363`，`471`）中干员发言又带了 `"type":"dialogue"`：

```json
[{"speaker":"成员甲","message":"台词内容","type":"dialogue"}, {"speaker":"旁白","message":"场景描述","type":"narration"}]
```

**影响**：模型会困惑「到底要不要 type 字段」。建议统一加上 type。

### 问题 2：群聊线下/导演「旁白必须」与想要的「旁白可选」矛盾

**位置**：`PromptTemplates.kt:359,371`（线下），`467,479`（导演）

```
本轮必须包含至少一条旁白条目
每位当前成员必须至少产生一条真实台词；本轮必须至少一条第三人称旁白
```

但想要的规则是「旁白可有可无」。需要改为「可以包含旁白」或「建议包含旁白」。

### 问题 3：群聊线下/导演字数限制冲突

**位置**：`PromptTemplates.kt:365,370`（线下），`473,478`（导演）

```
第365行：每条消息{{GROUP_MSG_MIN}}~{{GROUP_MSG_MAX}}字
第370行：旁白时填场景描述...{{GROUP_NAR_MIN}}~{{GROUP_NAR_MAX}}字
```

同一条消息，是受 GROUP_MSG 限制还是受 GROUP_NAR/GROUP_DIA 限制？需要明确优先级。

### 问题 4：群聊每个角色至少发言一次的表述含「未禁言」

**位置**：所有群聊模板（`415/524/644/752`）

```
当前成员名单中的每位未禁言成员本轮都必须至少说一句真实台词
```

当前代码已经通过过滤 `mutedIds` 来生成 `activeMembers`，提示词里的「未禁言」字眼是多余的。应该简化为「每位成员本轮至少说一句台词」，不必再提禁言。

### 问题 5：私聊线下/导演「至少一段旁白和对话」+「最后一段必须是 dialogue」

**位置**：`PromptTemplates.kt:69,159`

```
至少一段 narration 和至少一段 dialogue；最后一段必须是 dialogue
```

这个要求本身没问题，但需要清楚：**提示词写要求，代码不强制执行**。模型不遵守时，不补内容、不报错，直接展示已有的内容。

### 总结提示词修改清单

| 模板 | 行号 | 改什么 |
|---|---|---|
| 群聊线下 | 359, 371 | 「必须旁白」→「可以包含旁白」 |
| 群聊线下 | 370 | type 字段说明统一（全部加 type 或全部按示例来） |
| 群聊导演 | 467, 479 | 同上 |
| 群聊导演 | 478 | type 字段说明统一 |
| 群聊全部 | 415/524/644/752 | 去掉「未禁言」字眼 |
| 群聊全部 | 365/473/703 | 明确字数限制优先级 |

---

## 四、程序部分的问题与修改方案

### 整体流程（现状与期望对比）

```
现状：
模型返回 → cleanJson格式清理 → 5级回退链(第5级编造内容)
         → 私聊ensureVisiblePrivateReply(硬塞对话/不拦截空结果)
         → visiblePrivateSegmentCount(强撑为1) → 存库


期望：
模型返回 → cleanJson格式清理 → 前4级回退(不编造)
         → 格式正常? 存库 | 格式有问题? 重试1次 → 成功? 存库 | 失败? 灰色系统提示
```

### 修改方案详解

---

#### 修改 1：删除解析兜底第 5 级硬编码

**位置**：`AIService.kt:78-80`

**现状**：
```kotlin
val safe = raw.trim().lineSequence().firstOrNull { it.isNotBlank() && !looksLikeJson(it) }?.take(240)
    ?: "刚才那条消息格式有点乱，我重新整理一下再说。"
return OfflineModeResponse(segments = listOf(Segment(type = "dialogue", content = safe)))
```

**改为**：不走兜底，直接抛异常（触发上层重试机制）。

---

#### 修改 2：增加「格式异常→重试1次→失败则灰色系统消息」的统一机制

**位置**：`ChatViewModel.kt:775-776`（私聊主调用处）

**现状**：
```kotlin
var parsed = withTimeout(90_000) { sharedUtils.chatWithRetry(apiMessages) }
parsed = ensureVisiblePrivateReply(parsed, mode)
```

**改为**：
```kotlin
val parsed = withTimeout(90_000) { sharedUtils.chatWithRetry(apiMessages) }
// 不再调用 ensureVisiblePrivateReply，改为统一的格式检查
val validResult = validatePrivateReply(parsed, mode)
if (!validResult.isValid) {
    // 格式有问题（线上无dialogue / 完全空内容）
    // 显示灰色系统消息 "通讯出现波动，再发一遍吧"
    // 不存库、不给奖励
    // 注意：这里已经是 chatWithRetry 重试完的结果，不再额外重试
}
```

同时 `chatWithRetry`（`AIService.kt:404-450`）也需要改：
- 当 `normalizeOfflineResponse` 走到底（第6层）时，不要返回硬编码，而是抛异常，让重试机制再请求一次
- 如果重试完还是失败，则把空/无效状态返回给上层

---

#### 修改 3：删除私聊硬塞对话逻辑

**位置**：`ChatViewModel.kt:626-630`

**现状**：
```kotlin
if (normalized.lastOrNull()?.type != "dialogue") {
    val fallback = parsed.dialogue.ifBlank { "嗯，我在听。" }
    normalized += com.rhodes.privatechat.shared.model.Segment("dialogue", fallback)
}
```

**改为**：整段删除，末段是旁白就旁白，什么都不补。

---

#### 修改 4：修改私聊可见段计数，去掉强撑

**位置**：`ChatViewModel.kt:586-590`

**现状**：
```kotlin
return count.coerceAtLeast(if (parsed.dialogue.isNotBlank()) 1 else 0).coerceAtLeast(1)
```

**改为**：
```kotlin
return count // 真实计数，不强制≥1
```

这样当线上模式收到纯旁白时，`dialogue` 过滤后 count = 0 → `hasVisibleReply = false` → 不走存库/奖励。

---

#### 修改 5：私聊线上无 dialogue 的处理

**位置**：`ChatViewModel.kt:603-607`

**现状**：
```kotlin
if (mode == "online") {
    val dialogue = source.filter { it.type == "dialogue" }
        .ifEmpty { ... }
    return if (dialogue.isEmpty()) parsed else parsed.copy(segments = dialogue)
}
```

线上过滤旁白后 dialogue 为空时直接返回原 `parsed`。由于修改 4 去掉了强撑，`hasVisibleReply` 会变成 false，系统不会存库。但为了体验更好，应该明确输出一条灰色系统消息。具体在 `ChatViewModel.kt:812-820` 的 `else` 分支中处理。

---

#### 修改 6：群聊删除「每人必须发言」的结构校验

**位置**：`GroupChatViewModel.kt:897-905`

**现状**：
```kotlin
private fun hasRequiredGroupTurn(results, activeNames, mode): Boolean {
    if (results.isEmpty()) return false
    val everyMemberSpoke = activeNames.all { name ->
        results.any { it.speaker == name && it.type == "dialogue" && it.message.isNotBlank() }
    }
    if (!everyMemberSpoke) return false        // ← 删除
    val hasNarration = results.any { ... }
    return if (mode == "online") !hasNarration else hasNarration
                                          // ↑ 只保留线上过滤旁白
}
```

**改为**：
```kotlin
private fun hasRequiredGroupTurn(results, activeNames, mode): Boolean {
    if (results.isEmpty()) return false
    return if (mode == "online") results.none { it.type == "narration" } else true
    // 线上：去掉旁白（旁白条目本来就不展示）
    // 线下/导演：什么校验都不要，直接过
}
```

---

#### 修改 7：群聊删除两轮重试中的结构修复逻辑

**位置**：`GroupChatViewModel.kt:657-681`

**现状**：两轮生成 + 修复指令重试（针对缺成员缺旁白）

**改为**：只生成一轮，不做结构修复重试。如果模型返回的内容格式不对（非 JSON 等），走 API 重试（已有的 `chatWithRetry` 机制）。如果 API 重试完还是不行，存灰色系统消息。

---

#### 修改 8：统一「通讯出现波动，再发一遍吧」的灰色系统消息

**位置**：多处需要统一

**格式**：
```kotlin
ChatMessage(
    senderName = "系统",
    content = "通讯出现波动，再发一遍吧",
    type = "system",
    mode = mode,
    isMe = false
)
```

UI 渲染（已有代码 `MessageParser.kt:52-53`）：
- `type == "system"` → 调用 `systemMsg()` → 灰色文字展示在聊天记录中

改动涉及以下场景：
| 场景 | 当前行为 | 改为 |
|---|---|---|
| 解析完全失败+重试仍失败 | 硬编码「刚才那条消息格式有点乱」 | 「通讯出现波动，再发一遍吧」 |
| 线上无 dialogue | bug：存了不显示 | 「通讯出现波动，再发一遍吧」 |
| API 超时 | 「模型响应超时」（已接近） | 统一用「通讯出现波动，再发一遍吧」 |
| API 其他错误 | 错误文案 | 统一用「通讯出现波动，再发一遍吧」 |
| 群聊重试后仍失败 | 「群聊回复格式错误，请重试」 | 「通讯出现波动，再发一遍吧」 |

---

### 不改的部分

| 代码 | 位置 | 理由 |
|---|---|---|
| `cleanJson()` | AIService.kt:201-242 | 只修格式不编造，保留 |
| 回退链前 4 级 | AIService.kt:60-76 | 从原文提取字段，保留 |
| `normalizeSegmentType` 字段别名映射（content/message/text） | 各处 | 字段名不标准化时的容错，保留 |
| 私聊括号动作抽成旁白 | ChatViewModel.kt:610-624 | 从原文提取已有信息，不编造新内容 |
| 群聊 `normalizeGroupResults` 丢弃首人称旁白 | GroupChatViewModel.kt:789 | 合理过滤 |
| 群聊 `normalizeGroupResults` 线上丢弃旁白 | GroupChatViewModel.kt:790 | 合理过滤 |
| 禁言成员过滤 | GroupChatViewModel.kt:474-475 | 禁言成员不参与群聊，保留 |

---

### 修改影响范围总结

| 修改编号 | 影响文件 | 改动量 | 风险 |
|---|---|---|---|
| 1 | `AIService.kt` | 删几行 | 低 |
| 2 | `AIService.kt` + `ChatViewModel.kt` | 中等 | 中（涉及重试链路） |
| 3 | `ChatViewModel.kt` | 删几行 | 低 |
| 4 | `ChatViewModel.kt` | 一行 | 低 |
| 5 | `ChatViewModel.kt` | 已有 else 分支，改条件即可 | 低 |
| 6 | `GroupChatViewModel.kt` | 函数重构 | 低 |
| 7 | `GroupChatViewModel.kt` | 中等 | 中（群聊生成逻辑变化） |
| 8 | 多处统一文案 | 低 | 低 |

总共有 **8 处修改**，其中高风险 0 处，中风险 2 处（重试链路和群聊生成逻辑变更），低风险 6 处。

---

本文档覆盖了你提出的全部问题。如有遗漏或需要调整的地方，请指出。
